# we could just do intname = 8 but thats for dumb stupid scrubs

defmodule interger_stuff do
  def intergercomplicated do
    intergerhere = String.length("funni bd")
      IO.puts(intergerhere)
    end
end

interger_stuff
